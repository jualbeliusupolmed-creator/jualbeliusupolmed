# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — JANGAN commit nilai rahasia. Semua .env diabaikan kecuali template.
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# PWA (auto-generated oleh next-pwa saat build — jangan di-commit)
public/sw.js
public/sw.js.map
public/workbox-*.js
public/worker-*.js

# Data percakapan pelanggan & screenshot — JANGAN commit (privasi)
/Pelajari wa/
/Rob - *.jpg

# Laporan keamanan/audit — JANGAN commit (berisi detail celah sistem produksi)
docs/AUDIT-*
docs/Laporan-Keamanan.*
docs/Checklist-Rotasi-Kunci.*

# Folder kerja bot WA yang disalin dari VPS. Kodenya boleh ikut; BERKAS
# STATE-nya tidak. contacts.json / lid_resolution_map.json / messages.json
# berisi nomor telepon dan isi chat pelanggan sungguhan — repo ini publik.
# Pernah ikut ter-commit sekali (21 Agustus 2026) dan harus ditulis ulang.
bot-wa/*.json
!bot-wa/package.json
!bot-wa/package-lock.json
bot-wa/*.log
bot-wa/.bot.lock
bot-wa/.penjaga-*
bot-wa/auth_info_baileys/
bot-wa/push-ke-github.sh
# Dokumen atau dashboard yang memuat kredensial tidak boleh berada di worktree.
bot-wa/INFRASTRUKTUR_DAN_KREDENSIAL.md
bot-wa/halaman/infrastruktur.html
**/*kredensial*.md
**/*credentials*.md

# Catatan pribadi — namanya sudah bilang sendiri. Sempat terlacak
# 20 Jul–21 Agu 2026; isinya harus dianggap bocor dan dirotasi.
catatan-rahasia-lokal.txt
.env*
supabase/.temp/

# Coretan kerja lokal — bukan bagian aplikasi.
scratch/

# Arsip salinan env lama — berisi kredensial, jangan pernah di-commit.
arsip-env/

# Artefak kerja alat browser & tangkapan layar sementara — bukan bagian aplikasi.
.playwright-mcp/
/ui-home-*.png

# ── Artefak kerja yang sempat ter-commit (dibersihkan 28 Agustus 2026) ────────
# Repo ini PUBLIK. Yang terlanjur masuk di sini bukan rahasia — sudah diperiksa,
# tidak ada kredensial, dan nomor WA di dalamnya cuma placeholder — tapi 4,6 MB
# tangkapan layar acak dan transkrip percakapan AI membuat `git clone` berat dan
# akar repo tidak bisa dibaca sekilas.
#
# Barisnya ditinggal sebagai jaring: kalau berkas serupa dibuat lagi oleh alat
# mana pun, ia tidak ikut ter-commit.
lint-results.txt
isi.md
terakhir.md
/ggg.png
/sssss.png
/image.png
Screenshot */
*.tmp.png
